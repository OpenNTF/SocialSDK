CDNJS now fetch libraries out of the github tree for security reason.

this files are to be manualy updated after each release build but before the source tree is tagged.

CDNJS Will read these files and will generate a version for any tag it finds on the file history