package com.ibm.sbt.services.client.connections.files.util;
/*package com.ibm.sbt.services.client.connections.files;

import java.text.MessageFormat;
import java.util.ResourceBundle;

import javax.servlet.http.HttpServletRequest;

import com.ibm.wps.logging.LogManager;
import com.ibm.wps.logging.Logger;

public class internationalization {

	private static final Logger log = LogManager.getLogManager( ).getLogger( internationalization.class);

	public static String getParamString(String textKey, String[] params, HttpServletRequest req) {

		
		String rtval = MessageFormat.format(bundle.getString(textKey), (Object[])params);
		        
	    if (log.isLogging(Logger.TRACE_MEDIUM)) {
	    	log.exit(Logger.TRACE_MEDIUM, "getParamString , textKey=["+textKey+"] and text for message = " + rtval);
	    }
		return rtval;
	}	
}
*/